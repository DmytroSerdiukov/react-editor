import Box from "@mui/material/Box";

const Preview = () => {
  return (
    <Box
      style={{
        display: "flex",
        flexDirection: "column",
        height: "100%",
        width: "50%",
      }}
    >
      dsds
    </Box>
  );
};

export default Preview;
