import Container from "@mui/material/Container";

const Preview = () => {
  return (
    <Container
      style={{
        display: "flex",
        flexDirection: "column",
        height: "100%",
        background: "red",
      }}
    ></Container>
  );
};

export default Preview;
